@{
  Layout = "_Layout";
}

@using RecipeBook.Models;

<h1>Categories</h1>
<hr />

@if (@Model.Count == 0)
{
  <h3>No categories have been added yet!</h3>
} 

@foreach(Category category in Model)
{
  <p>@Html.ActionLink($"{category.Name}", "Details", new { id = category.CategoryId })</p>
}

